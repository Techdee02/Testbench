# Leave both unset for local dev — the app calls its own mock API routes
# under /api/* (see src/lib/store.ts) which match the Floater's contract
# exactly. Fill these in once Infra hands over the real values.

NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_R2_PUBLIC_BASE=
